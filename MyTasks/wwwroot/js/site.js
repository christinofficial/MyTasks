// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

//const { error } = require("jquery");

// Write your JavaScript code.
/*
function OnCreateFormSubmit(formId) {

    var descriptionField = $("#descriptionField");
    var targetDateField = $("#targetDateField");
    var errorMsgDescription = $("#errorMsgDescription");
    var errorMsgDate = $("#errorMsgDate");
    var isValid = true;

    if (descriptionField.val() === null || descriptionField.val() === '') {
        errorMsgDescription.text("Enter a description");
        errorMsgDescription.show();
        isValid = false;
    }
    else if (descriptionField.val().length < 11) {
        errorMsgDescription.text("Enter more than 10 chars");
        errorMsgDescription.show();
        isValid = false;
    }

    if (targetDateField.val() === null || targetDateField.val() === '') {
        errorMsgDate.text("Enter date");
        errorMsgDate.show();
        isValid = false;
    }

    if (isValid) {
        $('#' + formId).submit();
    }
}
*/